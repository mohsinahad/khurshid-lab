run = "npm run dev"
entrypoint = "src/app/page.tsx"

[nix]
channel = "stable-24_05"

[env]
PORT = "3000"

[deployment]
run = ["sh", "-c", "npm run start -- -p 3000 -H 0.0.0.0"]
build = ["sh", "-c", "npm install && npm run build"]
deploymentTarget = "autoscale"

[[ports]]
localPort = 3000
externalPort = 80
